# Call Center AI - 2025 現代化架構藍圖

## 🎯 架構願景

打造業界領先的低延遲、高效能、成本優化的 AI 呼叫中心平台。

---

## 🏗️ 整體架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                         用戶層                                     │
│                                                                   │
│  📞 PSTN 通話  │  💬 SMS  │  🌐 API  │  📱 WebRTC               │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    智能路由層 (NEW)                                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  VoiceRouter - 根據場景選擇最佳處理方式                    │    │
│  │  ├─ 低延遲需求 → Realtime API (200-300ms)              │    │
│  │  ├─ 成本優先 → 混合管道                                 │    │
│  │  └─ 複雜推理 → o3-mini                                  │    │
│  └─────────────────────────────────────────────────────────┘    │
└────────────────────────────┬────────────────────────────────────┘
                             │
        ┌────────────────────┴────────────────────┐
        │                                         │
┌───────▼──────────┐                    ┌────────▼─────────┐
│  🆕 Realtime 路徑  │                    │  傳統路徑 (降級)   │
│  (主要流程)       │                    │  (備用方案)       │
└───────┬──────────┘                    └────────┬─────────┘
        │                                         │
┌───────▼──────────────────────────────────┐     │
│  OpenAI Realtime API                     │     │
│  ├─ gpt-4o-realtime-preview             │     │
│  ├─ 端到端語音處理                       │     │
│  ├─ 原生工具調用                         │     │
│  └─ 延遲: 200-300ms                     │     │
└───────┬──────────────────────────────────┘     │
        │                                         │
        │              ┌──────────────────────────▼──────────┐
        │              │  傳統管道                            │
        │              │  ┌──────────────────────────────┐  │
        │              │  │ STT: gpt-4o-transcribe      │  │
        │              │  └──────────────────────────────┘  │
        │              │            ↓                        │
        │              │  ┌──────────────────────────────┐  │
        │              │  │ LLM: gpt-4o / o3-mini       │  │
        │              │  └──────────────────────────────┘  │
        │              │            ↓                        │
        │              │  ┌──────────────────────────────┐  │
        │              │  │ TTS: Azure Neural TTS       │  │
        │              │  └──────────────────────────────┘  │
        │              └─────────────────────────────────────┘
        │                              │
        └──────────────┬───────────────┘
                       │
┌──────────────────────▼───────────────────────────────────────────┐
│                      核心服務層                                    │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  RAG 引擎 (混合向量搜尋) 🆕                                │   │
│  │  ┌─────────────────┐    ┌─────────────────────┐         │   │
│  │  │ Redis Vector    │───▶│ Cosmos DB Vector    │         │   │
│  │  │ (熱快取)         │    │ (完整資料集)         │         │   │
│  │  │ < 5ms           │    │ < 20ms              │         │   │
│  │  │ 成本: 低         │    │ 成本: Pinecone的1/43│         │   │
│  │  └─────────────────┘    └─────────────────────┘         │   │
│  │                                                           │   │
│  │  智能切換:                                                 │   │
│  │  1. 先查 Redis (常見問題)                                  │   │
│  │  2. 未命中查 Cosmos DB                                     │   │
│  │  3. 自動提升熱門內容到 Redis                                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  工具執行引擎                                              │   │
│  │  ├─ end_call() - 結束通話                                 │   │
│  │  ├─ transfer_call() - 轉接人工                            │   │
│  │  ├─ new_claim() - 建立聲明                                │   │
│  │  ├─ send_sms() - 發送簡訊                                 │   │
│  │  └─ recall_time() - 設定提醒                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  對話狀態管理                                              │   │
│  │  ├─ Cosmos DB (持久化)                                    │   │
│  │  └─ Redis (工作階段快取)                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼────────────────────────────────────┐
│                      持久化層                                      │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ Cosmos DB 🆕     │  │ Redis Stack 🆕   │  │ Storage Queue│  │
│  │                  │  │                  │  │              │  │
│  │ • 通話記錄        │  │ • 熱快取          │  │ • 非同步任務  │  │
│  │ • 聲明資料        │  │ • 向量搜尋        │  │ • 事件佇列    │  │
│  │ • 向量索引 (新)   │  │ • 工作階段        │  │              │  │
│  │ • < 20ms 延遲    │  │ • < 5ms 延遲     │  │              │  │
│  │ • DiskANN        │  │ • VSS 模組       │  │              │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
└───────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼────────────────────────────────────┐
│                      監控和可觀測性 (增強) 🆕                       │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  OpenTelemetry 追蹤                                        │   │
│  │  ├─ 延遲追蹤 (首字節、總延遲、各階段)                         │   │
│  │  ├─ 成本追蹤 (每通電話、token使用)                           │   │
│  │  ├─ 品質追蹤 (中斷率、轉接率、完成率)                         │   │
│  │  └─ 錯誤追蹤 (超時、API失敗)                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Application Insights                                     │   │
│  │  ├─ 實時儀表板                                             │   │
│  │  ├─ 告警規則                                              │   │
│  │  └─ 自動縮放觸發器                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────┘
```

---

## 🔄 資料流程

### 主要流程：Realtime API (推薦)

```
1. 📞 用戶發起通話
   └─> Azure Communication Services 接收

2. 🔀 智能路由判斷
   └─> 選擇 Realtime 路徑（低延遲需求）

3. 🌐 建立 WebSocket 連接
   └─> OpenAI Realtime API (gpt-4o-realtime-preview)

4. 🎤 音訊串流處理
   ├─> 用戶語音 → Realtime API
   ├─> API 即時處理（語音理解）
   ├─> 查詢 RAG（如需）
   │   ├─> Redis 熱快取 (< 5ms)
   │   └─> Cosmos DB 向量 (< 20ms)
   ├─> 執行工具調用（如需）
   └─> 語音回應 → 用戶

5. 💾 狀態持久化
   ├─> Cosmos DB（通話記錄、訊息）
   └─> Redis（工作階段快取）

6. 📊 遙測記錄
   └─> OpenTelemetry → Application Insights

總延遲: 200-300ms (端到端)
```

### 降級流程：傳統管道

```
1. 📞 用戶發起通話
2. 🎤 STT: gpt-4o-transcribe (50-100ms)
3. 🤖 LLM: gpt-4o 處理 (100-200ms)
4. 🔍 RAG 查詢（如需）(20-50ms)
5. 🛠️ 工具執行（如需）(50-100ms)
6. 🗣️ TTS: Azure Neural TTS (100-200ms)
7. 📢 回應用戶

總延遲: 500-800ms
```

---

## 🎛️ 配置範例

### 新配置檔案 (config.yaml)

```yaml
# 語音處理配置
voice:
  # 主要處理方式
  primary:
    provider: openai_realtime
    model: gpt-4o-realtime-preview
    voice: alloy
    temperature: 0.7
    max_tokens: 4096

  # 降級選項
  fallback:
    provider: azure_traditional
    stt_model: gpt-4o-transcribe
    llm_model: gpt-4o
    tts_voice: zh-TW-HsiaoChenNeural

  # 路由策略
  routing:
    strategy: smart  # smart | always_realtime | always_traditional
    latency_threshold_ms: 300
    cost_threshold_per_call: 0.20

# LLM 配置
llm:
  # 對話模型
  chat:
    fast:
      deployment: gpt-4o-realtime-preview
      max_tokens: 2048
    standard:
      deployment: gpt-4o
      max_tokens: 4096

  # 推理模型
  reasoning:
    deployment: o3-mini
    version: "2025-01-31"
    use_cases:
      - complex_claim_analysis
      - dispute_resolution
      - policy_interpretation

  # 轉錄模型
  transcription:
    primary: gpt-4o-transcribe
    fallback: gpt-4o-mini-transcribe

  # 嵌入模型
  embedding:
    deployment: text-embedding-3-large
    dimensions: 3072

# RAG 配置（混合向量搜尋）
rag:
  strategy: hybrid  # hybrid | redis_only | cosmos_only

  # Redis 熱快取
  hot_cache:
    provider: redis
    host: ${REDIS_HOST}
    port: 6379
    ssl: true
    vector_index:
      name: rag_hot_cache
      dimensions: 3072
      algorithm: FLAT
      distance_metric: COSINE
    ttl_seconds: 3600  # 1小時
    max_items: 10000   # 最多快取項目

  # Cosmos DB 完整儲存
  cold_store:
    provider: cosmos_db
    database: call_center_ai
    container: knowledge_base
    vector_index:
      enabled: true
      type: diskANN
      similarity: cosine
    partition_key: /category

  # 搜尋參數
  search:
    top_k: 5
    min_similarity: 0.7
    rerank: true

  # 快取提升策略
  promotion:
    threshold_hits: 10  # 查詢10次後提升到熱快取
    auto_promote: true

# 資料庫配置
database:
  cosmos_db:
    endpoint: ${COSMOS_ENDPOINT}
    key: ${COSMOS_KEY}
    database: call_center_ai
    containers:
      - name: calls
        partition_key: /phone_number
        ttl: 2592000  # 30天
      - name: claims
        partition_key: /claim_id
      - name: knowledge_base
        partition_key: /category
        vector_enabled: true  # 🆕 啟用向量索引

    # 效能配置
    performance:
      autoscale_max_ru: 4000
      connection_pool_size: 20
      request_timeout: 30

# 快取配置
cache:
  redis:
    host: ${REDIS_HOST}
    port: 6379
    ssl: true
    password: ${REDIS_PASSWORD}

    # 連接池
    connection_pool:
      max_connections: 50
      socket_timeout: 5
      socket_keepalive: true

    # 快取策略
    strategies:
      session:
        ttl: 3600  # 1小時
      rag_results:
        ttl: 1800  # 30分鐘
      tts_audio:
        ttl: 86400  # 1天

# 監控配置（增強）
monitoring:
  application_insights:
    connection_string: ${APPINSIGHTS_CONNECTION_STRING}

  # 自訂指標
  metrics:
    - name: latency.first_token
      type: histogram
      unit: milliseconds
    - name: latency.total_call
      type: histogram
      unit: milliseconds
    - name: cost.per_call
      type: gauge
      unit: dollars
    - name: quality.interruption_rate
      type: gauge
      unit: percentage
    - name: rag.cache_hit_rate
      type: gauge
      unit: percentage

  # 告警規則
  alerts:
    - name: high_latency
      condition: latency.total_call > 1000
      action: scale_up
    - name: high_cost
      condition: cost.per_call > 0.50
      action: notify
    - name: low_cache_hit
      condition: rag.cache_hit_rate < 0.6
      action: optimize_cache

# 效能優化
performance:
  # FastAPI
  fastapi:
    workers: 4
    worker_class: uvicorn.workers.UvicornWorker
    json_serializer: orjson

  # 連接池
  connection_pools:
    http:
      pool_size: 100
      pool_maxsize: 200
      pool_block: false

  # 併發限制
  concurrency:
    max_concurrent_calls: 100
    max_concurrent_llm_requests: 50

  # 串流配置
  streaming:
    enabled: true
    chunk_size: 1024
    buffer_size: 4096

# A/B 測試
experiments:
  enabled: true
  framework: custom

  tests:
    - name: realtime_vs_traditional
      variants:
        realtime:
          weight: 70  # 70% 流量
          config:
            voice.primary.provider: openai_realtime
        traditional:
          weight: 30  # 30% 流量
          config:
            voice.primary.provider: azure_traditional

      metrics:
        - latency.total_call
        - cost.per_call
        - quality.completion_rate
```

---

## 📦 新依賴項

### pyproject.toml 更新

```toml
[project]
name = "call-center-ai"
version = "2.0.0"
requires-python = ">=3.13"

dependencies = [
    # FastAPI 和伺服器
    "fastapi ~= 0.115",
    "granian[reload] ~= 2.3",

    # JSON 加速 (新增)
    "orjson ~= 3.10",

    # OpenAI (升級)
    "openai >= 1.60.0",  # 支援 Realtime API

    # Azure SDK
    "azure-ai-inference ~= 1.0",
    "azure-communication-calling ~= 1.4.0a0",
    "azure-cognitiveservices-speech ~= 1.41",
    "azure-cosmos ~= 4.7",
    "azure-identity ~= 1.19",
    "azure-monitor-opentelemetry ~= 1.6",
    "azure-storage-queue ~= 12.12",

    # Redis Stack (升級以支援向量)
    "redis[hiredis] ~= 5.2",
    "redis-om ~= 0.3",  # 新增：Redis OM 支援

    # 資料驗證
    "pydantic ~= 2.9",
    "pydantic-settings ~= 2.6",

    # 非同步
    "aiojobs ~= 1.3",
    "aiohttp[speedups] ~= 3.10",
    "httpx[http2] ~= 0.28",  # 新增：HTTP/2 支援

    # 工具
    "tenacity ~= 8.2",
    "structlog ~= 24.4",
    "tiktoken ~= 0.8",
    "phonenumbers ~= 8.13",

    # 監控
    "prometheus-client ~= 0.21",  # 新增：Prometheus 指標
]

[project.optional-dependencies]
dev = [
    "pytest ~= 8.3",
    "pytest-asyncio ~= 0.24",
    "pytest-xdist ~= 3.6",
    "pyright ~= 1.1",
    "ruff ~= 0.7",
    "locust ~= 2.32",  # 新增：負載測試
]
```

---

## 🚀 遷移步驟

### 第一步：準備環境

```bash
# 1. 備份當前配置
cp config.yaml config.yaml.backup

# 2. 安裝新依賴
uv sync --all-extras

# 3. 升級 Azure 資源
make deploy-infrastructure
```

### 第二步：啟用 Cosmos DB 向量索引

```bash
# 執行遷移腳本
python scripts/migrate_cosmos_vector.py

# 驗證索引
python scripts/verify_vector_index.py
```

### 第三步：部署新程式碼

```bash
# 1. A/B 測試模式部署（30% 新架構）
make deploy-ab-test ratio=30

# 2. 監控指標
make monitor-ab-test

# 3. 逐步增加（50% → 70% → 100%）
make deploy-ab-test ratio=50
make deploy-ab-test ratio=70
make deploy-ab-test ratio=100
```

---

## 📊 預期改進

### 效能提升

| 指標 | 當前 | 優化後 | 改進 |
|------|------|--------|------|
| 端到端延遲 | 500-1000ms | 200-300ms | **60-70%↓** |
| 首字節時間 | 300-500ms | 100-150ms | **60%↓** |
| RAG 查詢延遲 | 50-100ms | 5-20ms | **75%↓** |
| 中斷恢復時間 | 2-3秒 | < 500ms | **80%↓** |

### 成本優化

| 項目 | 當前 (月) | 優化後 (月) | 節省 |
|------|----------|------------|------|
| LLM 成本 | $56.65 | $35.00 | **38%↓** |
| STT/TTS | $152.56 | $0 (整合) | **100%↓** |
| AI Search | $73.73 | $0 (移除) | **100%↓** |
| Cosmos DB | $233.60 | $180.00 | **23%↓** |
| Redis | $50 | $80 | 60%↑ (值得) |
| **總計** | **$720.07** | **$450.00** | **37%↓** |

### 品質提升

- ✅ 更自然的對話（支援中斷、情感）
- ✅ 更精準的工具調用
- ✅ 更好的多語言支援
- ✅ 更高的完成率（減少超時）

---

## 🎯 成功指標

### KPI 定義

```yaml
success_criteria:
  performance:
    p50_latency: < 250ms
    p95_latency: < 400ms
    p99_latency: < 600ms

  quality:
    completion_rate: > 95%
    transfer_rate: < 5%
    customer_satisfaction: > 4.5/5

  cost:
    cost_per_call: < $0.15
    total_monthly_cost: < $500

  reliability:
    uptime: > 99.9%
    error_rate: < 0.1%
```

---

## 📝 總結

這個 2025 現代化架構將為您的呼叫中心 AI 帶來：

1. **🚀 60-70% 延遲降低** - 從 500-1000ms 降至 200-300ms
2. **💰 37% 成本節省** - 從 $720/月 降至 $450/月
3. **⚡ 更好的使用者體驗** - 接近人類對話速度
4. **🔧 更簡化的架構** - 減少元件，提高可靠性
5. **📈 更強的可擴展性** - 支援更高併發和負載

準備好開始遷移了嗎？
